# Единые зависимости проекта (CONTRACT.md §2). Версии — в пределах мажоров стека.

# Модели данных (shared/)
pydantic>=2.0.0

# Парсер портала (parser/) и LLM-клиент (core/llm)
httpx>=0.27.0
tzdata>=2024.1            # zoneinfo Europe/Moscow на Windows

# Шифрование токена (core/security)
cryptography>=42.0

# Доступ к БД (db/)
supabase>=2.0.0

# Бот и планировщик (bot/)
aiogram>=3.4,<4
APScheduler>=3.10,<4

# Конфигурация
python-dotenv>=1.0.0

# Mini App сервер (mini_app/)
fastapi>=0.110.0
uvicorn>=0.29.0

# Тесты
pytest>=7.0.0
pytest-dotenv>=0.5.2
